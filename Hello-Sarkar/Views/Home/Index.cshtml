@model IEnumerable<News>

@{
    ViewBag.Title = "Latest News";
}

<div class="card-deck">
    @foreach (var news in Model)
    {
        <div class="card m-3">
            <div class="card-header">
                <h3>@news.Title</h3>
            </div>
            <div class="card-footer text-center">
                <a asp-controller="home" asp-action="details" asp-route-id="@news.Id" class="btn btn-primary">View</a>
            </div>
        </div>
    }
</div>
